# Phase 0 Checkpoint — Project scaffold

**Branch:** `phase/0-v1-build`
**Status:** Complete ✓
**Timestamp:** 2026-05-13T02:45:25Z

## What landed

- `LICENSE` — standard MIT, © 2026 Nick / perlantir
- `Diakonos/App/DiakonosApp.swift` — `@main` SwiftUI App, `WindowGroup("Diakonos")`, default size 1440×900 (matches design canvas), unified titlebar toolbar style
- `Diakonos/Design/Tokens.swift` — `DesignTokens` enum with `Palette` (every hex from `design/tokens.json` plus derived dark-mode variants), `Spacing`, `Radius`, `Shadow`. `dyn(light:dark:)` helper resolves dynamically via `NSColor(name:dynamicProvider:)`.
- `Diakonos/Design/Typography.swift` — `display(_:weight:)`, `text(_:weight:)`, `mono(_:weight:)` returning `Font` at token sizes (xs=12, sm=13, md=14, lg=16, xl=20, xxl=28, xxxl=34). SF Pro is the system font on macOS; SF Mono via `.monospaced` design.
- `Diakonos/UI/RootView.swift` — centered "Diakonos" title + secondary subtitle on `bgApp` background, min frame 1024×640.
- `Diakonos/Diakonos.entitlements` — Hardened Runtime entitlements (allow-jit, allow-unsigned-executable-memory, disable-library-validation, allow-dyld-environment-variables, apple-events). No App Sandbox.
- Directory skeleton for Phase 1+: `Diakonos/UI/{Components,Workspace,Workspace/Panes,Toolbar,Preferences}`, `Diakonos/Services`, `Diakonos/Models`, `Diakonos/Assets.xcassets/AppIcon.appiconset`, `Docs/Checkpoints/`.

## Verification

```
xcodegen generate               # → Diakonos.xcodeproj created
xcodebuild ... -scheme Diakonos build   # → ** BUILD SUCCEEDED **
open Diakonos.app               # → process running (PID 68042), window visible
```

App launched. macOS-native window with traffic lights, title "Diakonos", "Diakonos" display label centered, "Sandboxed AI agent workspace" secondary label below. Background swaps light/dark per system appearance. Hardened Runtime active (ad-hoc codesign disables it for local-run, real signing happens at distribution).

## Decisions made (within Step 2 latitude)

- **Workspace dropdown text:** "Diakonos" literal (not "cua Workspace"). Surfacing this in Phase 1 — flip if you'd rather.
- **Dark-mode tokens:** No explicit dark palette was in `tokens.json` beyond `bg.darkSurface` and `text.inverse`. Derived dark variants by sampling `design/screens/01-main-workspace.png` dark mockup:
  - `bgApp`: `#0B1220` (deepest layer)
  - `bgSurface`: `#111827` (matches token's `darkSurface`)
  - `bgElevated`: `#161E2E` (panel-elevated tier)
  - `textPrimary`: `#F9FAFB` (matches token's `inverse`)
  - `textSecondary`: `#C4CADA` / `textMuted`: `#8995AC`
  - `borderSoft`: `#222C42` / `borderDefault`: `#2A364F` / `borderStrong`: `#3A4663`
  - `accentSoft (dark)`: `#1E2A52` / `hermesOrangeSoft (dark)`: `#3A2A14`
  - Accents and status colors are constant across modes (Tailwind-like palette behavior).
- **LICENSE** added (MIT, your name). Surfacing in case you'd authored a different version.
- **Files registered via XcodeGen** (`sources: - path: Diakonos`), so adding new `.swift` files under `Diakonos/` does NOT require explicit registration — just re-run `xcodegen generate`. Locked invariant for the rest of the build.

## Issues / non-blockers

- macOS host is 26.0 SDK with Xcode 26.4.1 / Swift 6.3. `project.yml` declares `SWIFT_VERSION: 5.9` and `SWIFT_STRICT_CONCURRENCY: minimal`. Build clean — no concurrency diagnostics. Will revisit if Phase 2's actor + `Process` interaction surfaces strict-concurrency complaints.
- `appintentsmetadataprocessor` warns `No AppIntents.framework dependency found` — benign, we don't use AppIntents.
- `note: Disabling hardened runtime with ad-hoc codesigning` is expected for local-debug builds; the entitlements are still on the binary for proper sign-and-distribute later.

## What's next

Phase 1 — workspace shell. 2x2 pane grid, draggable dividers, pane headers, toolbar, static "Initializing" placeholders.
